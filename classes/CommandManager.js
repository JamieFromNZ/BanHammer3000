class CommandManager {
    constructor(bot) {
        // Map of all command files
        this.commandsMap = new Map();
    }

    loadCmds() {
        // Get all command files and store them in this.commandsMap;
    }
}

module.exports = CommandManager;