<%- include("partials/header") %>
<body>
    <section id="navbar">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">BanHammer3000</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="/dashboard">Dashboard</a>
                        </li>
                    </ul>
                    <% if (isLoggedIn) { %>
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <img src="<%= avatarURL %>" alt="User avatar" class="rounded-circle" width="30">
                                <span class="navbar-text ml-2"><%= username %></span>
                            </li>
                        </ul>
                    <% } else { %>
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link" href="/auth/discord">Login</a>
                            </li>
                        </ul>
                    <% } %>
                </div>
            </div>
        </nav>        
    </section>
    <section id="heading">
        <div class="container-fluid bg-image d-flex align-items-center">
            <div class="container text-center text-dark">
                <h1 class="display-2">BanHammer3000</h1>
                <p class="lead">A clan's plan for spam, it's the fan of the ban. No scam can withstand, it's the guardian of your land!</p>
            </div>
        </div>
    </section>
</body>
<%- include("partials/footer") %>